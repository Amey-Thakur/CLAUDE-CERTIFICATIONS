version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
  # The workflows pip install pinned versions, so those pins need watching too.
  - package-ecosystem: pip
    directory: /.github/workflows
    schedule:
      interval: monthly
